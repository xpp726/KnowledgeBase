fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
httpx>=0.27.0
pymilvus>=2.4.0
python-multipart>=0.0.9
aiofiles>=24.1.0

# 数据访问（SQLAlchemy 2.0 async + Alembic 迁移）
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0
asyncmy>=0.2.9
alembic>=1.13.0
# ⚠️ 连接 MySQL 8.4+/9.x 必需：caching_sha2_password 在非 TLS 直连下需 RSA 公钥交换，asyncmy 依赖此包
cryptography>=42.0.0

# 对象存储（MinIO，原始文档文件）
minio>=7.2.0

pymupdf4llm>=0.0.17
# ⚠️ onnxruntime 锁 1.29.0：后端 venv 已改用独立 Python 3.13(自带当前 MSVCP140 v14.44)，
# 不再受 Anaconda 旧运行库(v14.29)劫持，故可用最新版(支持 ONNX IR10 + numpy2)。
# 若回退到 Anaconda 作基解释器，必须锁 1.20.1 否则导入时 WinError 1114(DLL 初始化失败)。
onnxruntime==1.29.0
python-docx>=1.1.0
openpyxl>=3.1.5
python-pptx>=0.6.23
beautifulsoup4>=4.12.3
lxml>=5.3.0

pytest>=8.3.0
pytest-asyncio>=0.24.0

# 认证与权限（JWT + bcrypt）
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
